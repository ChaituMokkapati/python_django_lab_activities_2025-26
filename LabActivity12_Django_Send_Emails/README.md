# Lab Activity 12 - Application in Django to Send Emails

```bash
cd LabActivity12_Placeholder/EmailProject
python manage.py migrate
python manage.py runserver
```

Set real SMTP credentials in `EmailProject/settings.py` before testing email sending.
